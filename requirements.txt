# Core web framework
fastapi>=0.103.1,<1.0.0
uvicorn[standard]>=0.23.2,<1.0.0
python-multipart>=0.0.6

# Scientific computing and data processing
numpy>=1.21.0,<2.0.0
scipy>=1.7.0,<2.0.0
pandas>=1.3.0,<3.0.0

# 3D geometry and mesh processing
trimesh[easy]>=3.9.0,<5.0.0
pyiges[full]>=0.3.1
vtk>=9.0.0
geomdl>=1.0.0

# Advanced visualization
pyvista>=0.33.0,<1.0.0

# ⚠️  FEniCS packages MUST be installed via conda, NOT pip!
# These are NOT available on PyPI:
# fenics-dolfin  (install via: conda install -c conda-forge fenics-dolfin)
# fenics-mshr    (replaced with gmsh for better mesh generation)

# Mesh generation (available via both conda and pip)
gmsh>=4.8.0,<5.0.0

# Real-time communication
websockets>=10.0,<13.0
python-socketio>=5.0.0,<6.0.0
python-engineio>=4.0.0,<5.0.0

# Security and authentication
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.0,<2.0.0
python-dotenv>=0.19.0,<2.0.0

# Additional dependencies for enhanced FEM simulation
meshio>=5.0.0,<7.0.0
gmsh>=4.8.0,<5.0.0
